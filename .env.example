# ─────────────────────────────────────────────────────────────
# Docker Compose muhit o'zgaruvchilari (namuna).
# Ishlatish: ushbu faylni `.env` deb nusxalang va qiymatlarni o'zgartiring.
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────

# Ma'lumotlar bazasi
DB_USERNAME=nss_user
DB_PASSWORD=change_me_strong_password
DB_NAME=nss

# Frontend tashqi porti (server 80-portda ochiladi)
WEB_PORT=80
